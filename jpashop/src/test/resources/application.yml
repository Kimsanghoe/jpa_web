spring:
#  datasource:   #  별도의 설정이 없으면 자동으로 In-Memory 모드로 실행한다.
 #   url: jdbc:h2:mem:test
  #  username: sa
   # password:
    #driver-class-name: org.h2.Driver

#  jpa:
 #   hibernate:
  #    ddl-auto: create
   # properties:
    #  hibernate:
#          show_sql: true
     #   format-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
